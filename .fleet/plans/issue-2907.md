## Plan: render: canonical yaw-sweep jitter gate is RED on master at zoom 4/8 — #2469 accepted-residual table is stale

- **Issue:** #2907
- **Model:** opus
- **Date:** 2026-08-23

### Scope

Close out #2907 as a **measured attribution + gate re-grounding**, not a render fix. Since this issue was filed, #2606 (PRs #2922 + #2976, merged 2026-08-21) re-pointed the canonical rotation gate to the **pinned** `--yaw-sweep --pivot-origin` sweep with per-zoom `--max-excursion-x` bars, and #2641 (PR #2758, merged 2026-08-21) root-caused the default-focus orbit as inherent. What is still open on master `6eb34a21`:

1. Criterion 1 is unclosed — nobody has measured the residual growth against a specific change; the doc carries a "says that redness is orbit-inflated" inference (`engine/render/CLAUDE.md:664-667`, `:1109-1112`) with the z4 arm only.
2. Criterion 2 is unclosed — the #2469 accepted-residual table (`engine/render/CLAUDE.md:1087-1098`) still publishes the **unpinned** 2026-07-28 numbers as "accepted", with a blockquote disclaiming them; the residual criterion's paragraph (`:588-596`) still cites "measured 2026-07-28".
3. The doc says "#2641 → PR **#2758** (open, in review)" at `:655` and "(#2641 → #2758, in review)" at `:1102` — stale since 2026-08-21.
4. Criterion 4 has no recorded run: the canonical (pinned) probe's residual **and** excursion exit codes at zoom 2/4/8 on master.
5. `tools/jitter_probe/README.md:148-171` still says `--max-residual` is "the live assertion" with no pinned-probe floor and no unit note.

Deliverable: one PR (docs + plan file, no engine/shader code) that attributes the growth to commit `4c4554d5` (#2547's depth-aware default `CAMERA_CENTER` focus, 2026-07-31) on measurement, replaces the stale table with a pinned-probe residual table (voxel + SDF control + kill-switch arm, dated, host + scale recorded), keeps the `--max-residual` bar at the tool default 1.50 framebuffer px with the reason stated, fixes the stale #2758 references, and records the canonical probe green at all three zooms.

### Verified current state (source-checked on master `6eb34a21`; no new capture in this planning pass — phase 0 is the capture)

- **Pivot modes are exactly two**: `enum class RotationPivotMode { ORIGIN = 0, CAMERA_CENTER = 1 }` (`engine/render/include/irreden/render/ir_render_types.hpp:862`), default `CAMERA_CENTER` (`render_manager.hpp:183`). `getEffectiveCameraIso` (`engine/render/src/ir_render.cpp:45-80`) returns `cameraIso` unmodified under `ORIGIN` (`:47-49`); under the default it pivots about `getDefaultRotationPivotFocus()` — the point under the viewport center at the **latched composite-depth** (`render_manager.cpp:292-308`, derive `:310-397`). The derive's depth-0 fallback "is the pre-#2547 point exactly" (`render_manager.cpp:303-306`; `ir_render.cpp:68-70`).
- **The yaw sweep holds the camera at iso (0,0)** and steps yaw 0.05→0.70 rad over 24 shots (`creations/demos/shape_debug/main.cpp:1934-1968`, log line `Yaw-sweep: 24 shots, yaw 0.05->0.7 rad at cameraIso (0,0)`), and `--spin-shape` spawns its single fixture at the world origin (`engine/render/CLAUDE.md:544-549`). At camera iso (0,0) the depth-0 point under the viewport center **is** the world origin, so for this fixture the pre-#2547 default focus, the `--pivot-origin` arm, and an explicit focus at the origin all reduce to `cameraYawPivotOffset == 0` → `cameraIso`. **Code identity: the pinned arm on today's tree is the pre-#2547 default-pivot configuration of this probe.**
- **`jitter_probe`'s residual is the max-abs deviation from a least-squares line over frame index** (`tools/jitter_probe/main.cpp:138-163`, `:223-227`); `--max-residual` defaults to 1.50 (`:272`) and applies to whatever pixel unit the PNG carries — the tool has no host-scale normalization.
- **Pixel unit is host-scaled.** Captured PNGs are `outputScaleFactor` framebuffer px per game px, `floor(viewport / gameResolution)` in FIT mode (`render_manager.cpp:601-612`): **2 on the macOS HiDPI host (1280x720 game res → 2560x1440 PNG), 1 on Windows/Linux** (`scripts/pivot-verify.py:97-99`, `:130-146`, `:198-214`; `docs/design/camera-yaw-pivot.md:416`). Every number in the issue body, in the #2606 cross-comments, and in both CLAUDE.md tables is **macOS/Metal framebuffer px (2x)**. A 1x host reads ≈ half: the issue's 1.78/2.70 become ≈0.9/1.35 — *under* the 1.50 bar — so the raw-px "red" is a 2x-host fact, while the pinned-vs-unpinned differential below is host-independent.
- **Existing measurements this plan cites** (all macOS/Metal, 2x, 24-frame sweeps, arm-asserted): unpinned voxel x residual 0.85 / **1.78** / **2.70** at z2/4/8 (issue body 2026-08-05; reproduced to the digit 2026-08-07, #2606 cross-comment), unpinned x excursion 19.97 / 38.18 / 76.81; pinned voxel residual **0.41 / 0.08 / 0.03**, excursion 0.62 / 0.18 / 0.06 (2026-08-08, `engine/render/CLAUDE.md:619-623`, `:664-667`); unpinned SDF control residual 2.37 / 3.26 at z4/z8; `IR_PERAXIS_OVERFLOW_DISABLE=1` unpinned 1.06 / 1.88 (2026-08-07). The 2026-07-28 pre-#2547 table: voxel residual 0.85 / 0.57 / 1.25, excursion 1.68 / 1.26 / 2.83 (`CLAUDE.md:1093-1098`).
- **Arc signature already in the numbers.** A smooth orbit over a fixed angular span leaves a linear-fit residual **proportional to its chord**: the unpinned residual/excursion ratio is 0.043 / 0.047 / 0.035 at z2/4/8 — constant to coverage noise across a 4x zoom range — which is the signature of a circular-arc orbit under a line fit, not of a per-axis pixel-domain defect (those sit ~constant in px across zooms, #2469 finding 3, `CLAUDE.md:1130-1135`). z2's "unchanged 0.85" is a coincidence of two mechanisms (content+sampling pre-#2547; orbit post-#2547), not evidence of stability.
- **Window census.** Render-surface commits between the 2026-07-28 measurement and the #2547 merge `4c4554d5` (2026-07-31): `dce3d104` (#2546 P2, per-axis composite registration under non-integer effective camera offset), `53ac2cba` (#2605, the #2469 accept itself), `6cbdfe5f` (#2598 Metal bind). The only change to the **default pivot focus** in the whole window through master is `4c4554d5`; `816efbd5` (#2758) is harness + doc only (gates to the measurement; no derive change). The kill switch is live (`system_trixel_to_framebuffer.hpp:504`).
- **Sibling / in-flight reconciliation.** #2469, #2479 (#2850), #2547, #2606 (#2922, #2976), #2641 (#2758) all closed/merged. No open engine PR touches the jitter/pivot/residual surface (cache checked 2026-08-23). `docs/design/camera-yaw-pivot.md` does not reference the residual gate — not touched. Nothing here changes the #2606 excursion bar table or `scripts/pivot-verify.py`.

### Approach

One approach, committed: **attribute by measurement (same-tree differential + one-commit bracket), then re-ground the residual gate's documentation on the pinned probe.** No render code changes. All captures: `find <save_files>/screenshots -name '*.png' -delete` before every run, `--auto-screenshot 6`, score with `jitter_probe --reversal-eps 0.8 --expect-frames 24`, assert arm identity from the engine log (`RotationPivotMode: ORIGIN (--pivot-origin)` present/absent, `Spin-shape single fixture: Cylinder (voxel-pool)` vs `(SDF)`, `Yaw-sweep: 24 shots`, zoom as received). Record host, backend, date, and `outputScaleFactor` (PNG IHDR width ÷ `game_resolution_width`, the `_output_scale_factor` recipe) once per session; every number below is stated in **framebuffer px at the 2x host** — a 1x host expects ≈ half of each.

**Phase 0 — premise probe (same tree = the PR branch on master, one host, one session).** Five arms × three zooms (2/4/8) of the canonical cylinder fixture:

| arm | flags | expected (2x fb-px) x residual z2 / z4 / z8 |
|---|---|---|
| A unpinned voxel (the issue's probe) | `--spin-shape cylinder --spin-shape-voxel --yaw-sweep --zoom Z` | ≈0.85 / 1.78 / 2.70 (z4, z8 exit 1 under the 1.50 default); excursion ≈20 / 38 / 77 |
| B pinned voxel (the canonical gate since #2606) | A + `--pivot-origin` | ≈0.41 / 0.08 / 0.03, exit 0 |
| C pinned SDF control | B without `--spin-shape-voxel` | sub-0.5 (excursion 0.00 at z4/z8 on 2026-08-08; residual not yet on record — this run records it) |
| D unpinned SDF control | A without `--spin-shape-voxel` | ≈2.37 / 3.26 at z4/z8 (z2 not on record) |
| E pinned voxel + kill switch | `env IR_PERAXIS_OVERFLOW_DISABLE=1` + B flags | residual recorded only (not a bar source); excursion ≈6.2 / 10.9 / 21.8 |

Premise **confirmed** when all of: B ≤ 1.50 at every zoom; B ≤ the 2026-07-28 voxel residual (0.85 / 0.57 / 1.25) + 0.2 at every zoom; A/B ≥ 5 at z4 and z8; A's residual/excursion ratio lies in 0.03–0.06 at all three zooms (the arc signature). **Bail (refuted):** B > 1.50 at any zoom, or B exceeds the 2026-07-28 value at z4 or z8 by more than 0.2 fb-px → the per-axis path itself moved and the approach below does not apply: STOP, post the full 15-reading table on the issue, flip `fleet:queued` → `fleet:needs-plan` with "Plan stale: pinned residual regressed — <readings>" per PLANNING-PROTOCOL.md §Re-planning, release the claim. Do not proceed to the doc refresh on a refuted premise.

**Phase 1 — attribution bracket (one old-tree build).** Copy the master-built probe binary aside first (`fleet-build --target jitter_probe` on the PR tree, then `build/tools/jitter_probe/jitter_probe[.exe]` → `$HOME/.fleet/jitter_probe-master`); the 2026-07-31 tree predates `--expect-frames` (#2814, 2026-08-04) and every population in this PR is scored by the same binary. Then in the pool worktree: `git checkout 4c4554d5^` (the parent of the #2547 merge), `fleet-build --target IRShapeDebug`, capture arm A at z4 and z8 (2 runs; `--pivot-origin` exists there — #1352 — but the bracket needs the issue's unpinned probe), score with the copied binary, then `git checkout <PR branch>` and rebuild. Expected: residual ≈0.57 / 1.25 ± 0.2 (exit 0), excursion ≈1.3 / 2.8 — i.e. the 2026-07-28 table. Outcome: the residual growth brackets to exactly `[4c4554d5^, 4c4554d5]` on the default-focus change, and phase 0 shows the pivot focus is the only knob between a red arm and a green arm on the current tree — criterion 1 closes as **"root-caused to #2547's depth-aware default focus (`4c4554d5`): a deliberate camera-behaviour change whose focus orbit (inherent per #2641/#2758) rides the centroid and inflates the line-fit residual; the per-axis path did not regress — pinned it reads below the 2026-07-28 accepted values"**. Bail: the pre-#2547 tree also reads > 1.50 at z4/z8 → the growth predates #2547: STOP, post readings, flip to `fleet:needs-plan` naming `dce3d104` / `53ac2cba` / `6cbdfe5f` as the remaining window for the re-planner. If `4c4554d5^` fails to configure/build on the implementer's host, record the failure verbatim in the PR body and close criterion 1 on phase 0 + the code identity above; do not retry on another host.

**Phase 2 — `engine/render/CLAUDE.md` refresh** (line anchors as of `6eb34a21`):
- `:588-596` (reversal-criterion paragraph): keep the eps rationale; replace "measured 2026-07-28" with the phase-0 date and state that `--max-residual` (1.50 fb-px default) stays the live residual assertion **on the pinned probe**, pointing at the refreshed table; add one sentence that the bar is in framebuffer px (host `outputScaleFactor`, 2x on the macOS host, 1x on Windows/Linux), so a 1x host has 2x the game-px headroom.
- `:650-667`: "#2641 → PR **#2758** (open, in review)" → merged 2026-08-21, bias inherent, harness gated to the measurement; replace the "Free evidence for #2907" paragraph with the resolved statement (attributed to `4c4554d5`; pinned residual floor in the table below; unpinned sweep carries neither bar).
- `:1087-1112`: replace the 2026-07-28 table + blockquote with a **pinned-probe residual table**: rows voxel (B), SDF control (C), kill-switch arm (E, "recorded, not a bar source"); columns z2/4/8; a `bar` row = 1.50; units line (fb-px, host, backend, `outputScaleFactor`, date); one history row for the unpinned default-focus sweep (A, D: "NOT a gate — orbit-inflated, #2907") so the issue's numbers stay findable; a short provenance note: 2026-07-28 table → #2547 landed 2026-07-31 → unpinned grew 3.1x/2.2x → #2907 attributed it (arc signature + bracket) → canonical gate pinned since #2606. **Bar decision, stated in the doc:** `--max-residual` stays at the 1.50 tool default — the pinned healthy maximum (z2 voxel) sits ≥3x under it, and no per-zoom residual bars are published because no live defect arm fires on the residual criterion (the pre-#2427 face-pop that grounded 1.50 is fixed on master; the kill-switch arm is owned by the excursion bar) — the bar rule's "≤0.5x the defect" clause has nothing to evaluate, so per the rule's own omission clause nothing thinner is published.
- `:1114-1135` (three findings): keep 1 and 3 verbatim; rewrite finding 2 as bar-relative — "the pinned voxel residual sits ≥3x under the 1.50 fb-px bar at every zoom; the SDF control is recorded beside it and is **not** used as an upper bound, because its own floor is the one-game-pixel destination quantization (#2645)" — true whichever of B and C reads lower. `:1102` "(#2641 → #2758, in review)" disappears with the blockquote.

**Phase 3 — `tools/jitter_probe/README.md:148-171`**: re-point "leaves `--max-residual` as the live assertion" at the pinned probe + the CLAUDE.md table, and add a 3-line unit note (framebuffer px; host `outputScaleFactor` 2 vs 1; bounds calibrated on one host mis-scale on the other — cite `scripts/pivot-verify.py` `_output_scale_factor` as the read recipe). No tool code change.

**Phase 4 — acceptance runs on the PR tree (criterion 4 + positive-fire)**, logged in the PR body with arm-identity lines:
- Canonical gate: arm B at z2/4/8 with `--max-excursion-x 2.0 / 0.5 / 0.5` (the published bars) and the default `--max-residual` → exit 0 ×3.
- Positive-fire (i), residual criterion is live and discriminates the arms: on a 2x host, arm A z4 exits 1 under the default bar and exits 0 with `--max-residual 99` (attributable to the residual criterion, excursion unbarred); on a 1x host, `--max-residual 0.5` exits 1 on arm A z4 and exits 0 on arm B z4.
- Positive-fire (ii), flag live on the population it clears: arm B z2 with `--max-residual 0.1` exits 1 (≈0.41 on 2x, ≈0.2 on 1x).

Commit order: plan file first (`.fleet/plans/issue-2907.md`), then the doc edits. `attach-screenshots` does not apply (no render code or shader change — the PR body carries the probe logs instead); `optimize` skipped (docs); `simplify` runs via `commit-and-push`.

### Affected files

- `.fleet/plans/issue-2907.md` — this plan, first commit.
- `engine/render/CLAUDE.md` — §"Verifying temporal stability" residual paragraph (`:588-596`), the pinned-bar paragraph + #2758 references (`:650-667`), the #2469 table block + findings (`:1087-1135`).
- `tools/jitter_probe/README.md` — §"Accepted floors, and the model's blind spot" (`:148-171`): pinned-probe pointer + unit note.
- No engine/shader/tool code. `scripts/pivot-verify.py`, `docs/design/camera-yaw-pivot.md`, the #2606 excursion bar table: untouched.

### Acceptance criteria

1. PR body carries the phase-0 table: 5 arms × 3 zooms (x residual, x excursion, exit code), y residuals recorded, per-run arm-identity log lines, plus host / backend / `outputScaleFactor` / date — and the confirmed-premise checks (B ≤ 1.50 everywhere; B ≤ 2026-07-28 + 0.2; A/B ≥ 5 at z4, z8; A ratio 0.03–0.06).
2. Criterion 1 closed in the PR body with the phase-1 bracket readings at `4c4554d5^` (z4/z8 residual + excursion + exit code), naming `4c4554d5` as the change — or the recorded build failure and the phase-0 + code-identity closure.
3. Canonical pinned probe exits 0 at z2, z4, z8 with the excursion bars and default residual bar on the PR tree (log excerpts).
4. Positive-fire (i) and (ii) above with the exact commands and exit codes.
5. `rg -n "2758" engine/render/CLAUDE.md` shows no "open" / "in review" wording; the 2026-07-28 residual table is gone; the new table has a date ≥ 2026-08-23, host, backend, `outputScaleFactor`, voxel + SDF + kill-switch rows, and the bar-decision sentence.
6. `tools/jitter_probe/README.md` names the pinned probe and the framebuffer-px unit.
7. `fleet-build --target IRShapeDebug` green on the PR tree (the probes need it; docs-only diff otherwise).

### Gotchas

- **Units.** Never compare a 1x-host reading against the 2x-host tables without stating the scale; `jitter_probe` cannot tell. Read `outputScaleFactor` off a captured PNG, state it once, and keep every figure in this PR in framebuffer px with that scale named. The unpinned arm's bar crossing only reproduces in raw px on a 2x host — on a 1x host the discriminators are the A/B ratio and the arc-signature ratio, both host-independent.
- **Wipe, count, assert.** Wipe the screenshot dir before every capture (the engine numbers around leftovers); `--expect-frames 24` is the only guard against a widened glob; a silently unpinned arm reads ~200x higher on excursion and looks like a regression — read the arm from the log, not from the argv you think you passed (`CLAUDE.md:573-578`).
- **Kill-switch form.** `env IR_PERAXIS_OVERFLOW_DISABLE=1 fleet-run IRShapeDebug …` (the `env` prefix is the gate-clean spelling on the Windows host). The unpinned kill-switch arm read *lower* residual than healthy on 2026-08-07; do not read arm E's residual as a bar source in either direction.
- **Old-tree build.** Stash the master probe binary before `git checkout 4c4554d5^` — the rebuild overwrites `build/tools/jitter_probe/`. The checkout is in the pool worktree (detached); return to the PR branch and rebuild before phase 4, and make sure no phase-1 PNGs survive into phase 4's glob.
- **Don't touch the excursion table.** The `--max-excursion-x` bars (`CLAUDE.md:619-634`) are #2606's and stay byte-identical; this PR adds the residual table beside them. Don't re-derive per-zoom residual bars — decided above, with the reason the doc must carry.
- **Two stale #2758 mentions**, `:655` and `:1102`; the second goes with the blockquote. Also don't strip the `--pan-sweep` guidance — the residual criterion is shared with the pan probe.
- **Plan is engine-public** — engine terminology only.
- **Positive-fire thresholds are host-keyed** (0.5 on a 1x host for (i); 0.1 on either host for (ii)). State which host form was run.

