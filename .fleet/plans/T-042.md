# Plan: T-042 — start-next-task skill: stack-aware reset

Part of the 5-task stacked-PR vision stack from issue #289.
See `.fleet/plans/T-041.md` for full plan and gotchas.

**Depends on:** T-041 (commit-and-push stack-aware mode)

**Files:** `.claude/skills/start-next-task/SKILL.md`

Call `fleet-claim molecule resume <agent>` before resetting. If stdout names
a `T-NNN`, branch off the just-opened PR's head ref, not `origin/master`. If
stdout is empty, keep current behavior (reset to master).

**Acceptance:** after opening PR for first task in a 2-task stack, running
`start-next-task` lands on a branch whose merge-base is the just-opened PR's
head ref. After exhausting the stack, resets to `origin/master` as today.
